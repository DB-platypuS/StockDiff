# 创建者: PlatyPus
# 创建时间: 2026-09-20
# 作用: 声明 Git 忽略的构建产物与本地环境文件（bin/obj/.vs 等），
#       避免编译产物与个人配置进入版本库。

bin/
obj/
.vs/
*.user
*.suo
TestResults/
publish/

# 本地私有构建配置（可能含内网接口地址等敏感值），仅供本机发布时注入
Directory.Build.local.props
