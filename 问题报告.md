# /api/v1/stock/diff 接口性能问题报告

## 一、现象

客户端调用 `GET /api/v1/stock/diff?warehouse=all&compare_hold=false&compare_expiry=false`
单次耗时约 **53 秒**，而返回数据仅 **35 条**。

## 二、实测证据

### 1. 客户端侧计时（已排除客户端因素）

- HTTP 往返等待（服务端处理+传输）：**53,111 ms**
- 客户端自身（JSON 反序列化 + 表格渲染）：**77 ms**
  → 客户端占比 0.14%，客户端无问题。

### 2. 数据库侧计时（直连两库实测，只读）

| 数据库 / 表                                               | 检测项                | 耗时        |
| ----------------------------------------------------- | ------------------ | --------- |
| 方仓 <方仓数据库IP> / GM\_WareHouse / Inventory\_Listinfo | 连接                 | 成功        |
| 同上                                                    | COUNT(\*) = 10,784 | **9 ms**  |
| 立库 <立库数据库IP> / data\_Imotion / Log\_Mstore        | 连接                 | **35 ms** |
| 同上                                                    | COUNT(\*) = 53,197 | **24 ms** |
| 同上                                                    | SELECT TOP 5       | **7 ms**  |

## 三、结论

**两张表全表扫描均在 24 毫秒以内，数据库响应正常。**
53 秒的耗时发生在**后端应用逻辑**，与数据库、网络、客户端无关。

## 四、重点怀疑方向

1. **N+1 逐行查询 / 逐行外部关联**（最可能）：35 条结果耗时 53 秒，平均约 **1.5 秒/条**，
   高度符合「每条记录都要额外查一次库 / 调一次 WMS」的模式。
2. 单次请求的 SQL 执行次数请用 SQL Profiler / 慢查询日志统计，
   若数量级达万条以上，即可确诊。
3. 是否存在大表无条件联表、或每次请求全量重算无缓存。

### 字段溯源补充（解释「为什么必须逐行查」，作者直连两库核对所得）

对比接口 12 列与两库原表，**仓库侧 7 列可直接取列，WMS 侧则无法直接取**：

| 界面列    | 原表来源                                              | 复杂度       |
| ------ | ------------------------------------------------- | --------- |
| 物料编码   | 方仓 `SerialNumber` / 立库 `RID`                     | 直接取列      |
| 储位     | 方仓 `Loction` / 立库 `Palno`                         | 直接取列      |
| 仓库数量   | 方仓 `Qty_n` / 立库 `Quant`                           | 直接取列      |
| 仓库效期   | 方仓 `Batch` / 立库 `DueDate`                         | 直接取列      |
| WMS库位  | 立库 `WareHouseId`（`100B`）                          | 直接取列      |
| 仓库冻结   | 立库 `If_Back='已冻结'`                                | 简单判定      |
| WMS冻结  | 立库 `WMSPDALock`/`WMSRIDAlock` 非空                  | 简单判定      |
| **WMS数量** | 疑在 `Bmhri2WMSPD`（事件日志）                            | **需按时间取最新** |
| **WMS效期** | **两库内均无法复现该记录的值（见下方扫描实证）**                        | **指向库外 WMS 系统** |
| 异常种类   | 计算（疑似 冻结 > 效期 > 储位 优先级）                           | **需逐行计算**   |

关键佐证（可复现）：

1. **WMS 侧是事件日志，不是当前快照**：`Bmhri2WMS` / `Bmhri2WMSPD` 中同一 `RID` 存在多条记录，
   时间不同、库位不同。例：`K999992025072000028` 有 3 条，库位分别为
   `B0001250-G0702217` / `B0001250-G0102710` / `B0001250-G0300505`，
   需**按时间排序还原当前状态**。
2. **WMS效期在两库内均无法复现**（针对接口 `WMS效期=20260516`）：

   - **证伪「同物料另一行的 DueDate」**：物料 `IMPV13000020` 在立库全表**仅 2 行**
     （`R25041900103` / `DueDate=20271202`、`K999992025072000028` / `DueDate=20270720`），
     **均不等于** 接口的 `20260516`。
   - **全库字符列扫描 `20260516`** 的命中结果：`Bmhri2WMS.RID/json`（5360，均为 RID 内嵌日期，非效期字段）、
     `Log_Mstore.DueDate`（**仅 5 行**）、`Log_Mstore.ProductionDate`（86）、`Log_Mstore.Batch`（46）。
     其中 `DueDate` 仅 5 行，而接口中 `WMS效期=20260516` 出现于**多条**记录，**数量对不上**。

   → **结论：接口的 WMS效期既不来自 `Log_Mstore` 任何列，也无法由同物料关联得出，指向 WMS 系统自身的数据。**

**推论**：后端很可能在**逐行**去 WMS 系统（或事件日志）取 WMS 侧数据，
每行一次跨系统 I/O，35 行也需数十秒。这正是「数据库本身 24ms、接口却 53s」的根本原因。

> 备注：本次核对同时发现设计文档 12.3 节三处字段映射有误，
> 实际为 物料编码=`SerialNumber`/`RID`、储位=`Palno`、数量=`Qty_n`/`Quant`，
> 供后端与业务方对照时留意。

## 五、优化建议

1. 消除循环内查库/循环内调 WMS，改为**批量查询 + 内存关联**（一次查出全部 RID 的 WMS 侧数据）。
2. WMS 事件日志（`Bmhri2WMS` / `Bmhri2WMSPD`）**一次性取每条 RID 的最新记录**，避免逐行查询。
3. 差异结果做**服务端缓存**（库存差异非高频变更）。
4. 优化后目标：单次请求 **< 1 秒**（对齐登录接口水准）。

## 六、风险提示

客户端拉取超时上限为 **60 秒**，当前实测已达 53 秒，
余量仅 7 秒，服务端稍有波动即会触发客户端超时失败。

